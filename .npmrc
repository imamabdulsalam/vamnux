# cPanel builds this application with NODE_ENV=production.  Vite, TypeScript,
# esbuild, and the client build plugins remain devDependencies, so explicitly
# include them during npm install before npm run build.  The runtime stays in
# production mode through the existing start script.
include=dev
